/*
const path = require("path");
const path = require("dotenv").config({
   path: path.resolve(__dirname,`../${process.env.NODE_ENV}`) 
});

module.exports = {
    PORT: data.parsed.PORT,
    API: data.parsed.API_ROOT
}
    */